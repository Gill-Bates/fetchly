--extra-index-url https://download.pytorch.org/whl/cpu #pinned

# Kept alphabetical. markupsafe, numpy, pydantic and starlette are imported
# directly by app/ and would otherwise only arrive transitively (via jinja2,
# torch/essentia and fastapi) - pinned here so a direct import can never be
# left unsatisfied by an upstream that drops them.
beat-this>=1.1.0
essentia            # pinned
fastapi==0.141.1
gunicorn==26.2.0
httpx==0.28.1
jinja2==3.1.6
markdown==3.10.3
markupsafe==3.0.3
nh3==0.3.7
numpy==2.5.2
pydantic==2.13.5
python-multipart==0.0.32
slowapi==0.1.10
starlette==1.6.0
torch==2.13.0
uvicorn[standard]==0.52.4
uvicorn-worker==0.4.0
